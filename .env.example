# pi-dashboard 环境变量模板
#
# 复制到下面任一位置（三处都会加载，按顺序，先到先得）：
#   1) 由 PI_DASH_ENV_FILE 指定的文件（显式指定但不存在会报错）
#   2) <pi-dashboard 仓库>/.env          ← 本文件复制成 .env 就放这
#   3) <PI_CODING_AGENT_DIR | ~/.pi/agent>/dashboard.env   ← 机器级，适合 systemd/launchd
#
# 加载发生在 backend 启动时（backend/env-file.ts，见 guide/env-configuration.md），加载到的变量
# 会被每一个 pi 子进程继承（pi-manager 用 ...process.env 启动 slot），所以扩展也能看到。
# 已经存在于 shell 环境里的同名变量**不会被覆盖**。

# ── 用哪个 pi ────────────────────────────────────────────────────────────────
# 不设时 dashboard 优先用仓库内自带的官方 pi（node_modules/.bin/pi，缺少补丁扩展 API）。
# install-standalone.sh 会自动写这一行，指向它装的 fork 构建。
# PI_SCRIPT=/home/you/pi-stack/pi/bin/pi

# ── 扩展的数据落点（默认都在 $PI_CODING_AGENT_DIR 下）─────────────────────────
# 只在需要沿用旧位置时才设置：
# PI_TRACE_DIR=/home/you/.pi/agent/pi-traces      # trajectory-recorder 的 trace 目录
# PI_TIMING_DIR=/home/you/.pi/agent/pi-timing     # trajectory-recorder 的计时账本
# PI_OBSERVATION_DIR=/home/you/.pi/agent/archiv   # observation-pack 的大结果归档

# ── 模型凭证：按你实际用的 provider 留一条 ───────────────────────────────────
# 写在这里的好处：dashboard 和它启动的所有 pi 进程都能拿到，不用改 shell profile 或 systemd unit。
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# DASHSCOPE_API_KEY=sk-...        # bash-digest 默认摘要模型 dashscope/qwen3.8-flash 用它
# BEDROCK_MANTLE_PROFILE=...      # 需要 AWS_PROFILE 回退时用（见 run.sh）

# ── dashboard 自身 ──────────────────────────────────────────────────────────
# PI_DASH_PORT=7777